services:
  - type: web
    name: arc-detection-dashboard
    env: python
    buildCommand: pip install -r requirements.txt && python arc_detection_model.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
